spring:
  application:
    name: eureka
  datasource:
    url: jdbc:mysql://localhost:3306/bankingEureka
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  #    show-sql: true
  #    properties:
  #      hibernate:
  #        dialect: org.hibernate.dialect.MySQL8Dialect

server:
    port: 8082